<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.join_sport_app">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/join"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".uioperator.menu.ProfileOPT_Activity"></activity>
        <activity android:name=".ui.menu.MapUpdate_Activity" />
        <activity android:name=".ui.menu.UpDateActivityUser_Activity" />
        <activity android:name=".uioperator.menu.Manage_StadiumActivity" />
        <activity android:name=".ui.menu.Check_Status_JoinStadiumActivity" />
        <activity android:name=".uioperator.menu.Detail_JoinStadium_Activity" />
        <activity android:name=".uioperator.menu.Check_JoinStadiam_Activity" />
        <activity android:name=".ui.menu.Post_ProfileActivity" />
        <activity
            android:name=".ui.dashboard.Map_Detail_Activity"
            android:label="@string/title_activity_map__detail_" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".ui.menu.Detail_JoinStadiamActivity" />
        <activity android:name=".ui.notifications.Map_DetailActivity" />
        <activity android:name=".ui.notifications.JoinStadiam_Activity" />
        <activity android:name=".ui.notifications.Detail_Stadiam_Activity" />
        <activity android:name=".uiadmin.Admin_MainActivity" />
        <activity
            android:name=".uioperator.dashboard.MapsOperatorActivity"
            android:label="@string/title_activity_maps_operator" />
        <activity android:name=".uioperator.dashboard.Stadiam_Activity" />
        <activity
            android:name=".MainActivityOperator"
            android:label="@string/title_activity_main_operator" />
        <activity android:name=".ui.home.CommentActivity" />
        <activity
            android:name=".ui.dashboard.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".ui.menu.ActivityUser_Activity" />
        <activity android:name=".ui.menu.ProfileActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.dashboard.UpdateNumberActivity" />
        <activity android:name=".ui.dashboard.Create_Activity" />
        <activity android:name=".ui.login.RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name="com.tangxiaolv.telegramgallery.GalleryActivity" />
    </application>

</manifest>